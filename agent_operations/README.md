# Archived: agent_operations

This directory is archived. It holds the original Agent Operations course
content (module decks `m1.html`–`m6.html`, the matching PDFs, `index.html`,
`adk-response-caching.html`, and `prompt.md`).

It is superseded by [`../agent_ops/`](../agent_ops/), which is the current,
maintained version of the course. Make new edits there, not here.

This content is kept for reference only and is not updated.
