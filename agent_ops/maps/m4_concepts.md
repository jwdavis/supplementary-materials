# Module 4 — concept list

Delivery minutes (non-lab): **50** (brief) → page budget ≈ 50 / 2.5 = **20 content pages**

Baseline: google-adk **2.9.0** (PyPI wheel downloaded and read 2026-09-13), google-agents-cli **1.5.0**
(wheel read), google-cloud-aiplatform **1.165.1** (vendors the `agentplatform` SDK; wheel read).
The companion page `agent_operations/m4.html` was verified against 2.7.1 on 2026-08-25; everything it
dates is re-verified here, and one of its corrections (per-step trajectory averaging) is out of date on
2.9.0. Doc pages under `docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/`
and `adk.dev/evaluate/` were fetched 2026-09-13; they carry no launch-stage labels. The GA
announcement (developers.googleblog.com, 2026-07-31) names evaluations, online monitors, and
trace/session evaluation as GA and names no runtime.

Two things were measured rather than read, on 2026-09-13 in `jwd-gcp-demos`:
`scratchpad/m4_trace_import.py` imported one Cloud Trace trace from a local `adk web --otel_to_cloud`
run into an evaluation set, and `scratchpad/m4_score_run.py` scored it server-side. Both resources and
the GCS output were deleted afterward. ROUGE-1 figures on concept 6 were computed with `rouge_score`.

Names carried over from M2/M3: Priya and Marco; the refund agent with tools `get_order`,
`verify_shipment`, `issue_refund`; the weather agent from the M3 demos.

| # | Concept | Agent-specific? | Example | Verify against | Disposition | Pages |
|---|---|---|---|---|---|---|
| 1 | **A test checks correct execution of deterministic code. An eval grades the quality of a response and the path the agent took to produce it.** The path is the trajectory: which tools were called, with which arguments, in what order. The refund agent's instruction changes by one clause; the final reply is unchanged; the trajectory lost a step. No unit test sees it, because no code path changed. An eval replays a recorded conversation and grades the decision. Two properties make this necessary: the same input can produce a different trajectory on the next run, and correctness has several dimensions at once (trajectory, answer, safety, cost) | **yes**: non-determinism of model output; correctness that is multi-dimensional | **Actor:** Priya, editing the refund agent's instruction.<br>**Input:** "refund order 6041", before and after the edit.<br>**Mechanism:** before, the trajectory is `get_order → verify_shipment → issue_refund`; after, `get_order → issue_refund`. The final reply is "I've taken care of that" both times.<br>**Result:** an answer check passes. A trajectory check scores the second run 0 | M2 page 4 scenario (`agent_ops/m2-d.html`); `trajectory_evaluator.py` (2.9.0) | focus | 1 |
| 2 | **Model evaluation happens before the agent exists: it picks the model.** Pointwise scores one model against metrics; pairwise compares two candidates and picks one. The Gen AI evaluation service runs either over a prompt set with `client.evals.evaluate`, using the same rubric metrics the agent pages use later. After that decision the model id is a line in the repo, and the agent is what gets evaluated. The source deck's slides 4–11 (unit-testing an LLM-backed function; judging a marketing post) are generic LLM-application testing and get one sentence here | generic (model selection) — **say so**; agent-shaped only in that the model is an external dependency the team chooses and then pins by id | **Actor:** Marco, before writing agent code.<br>**Input:** 30 representative prompts; two candidate model ids.<br>**Mechanism:** `client.evals.evaluate` with `TEXT_QUALITY` and `INSTRUCTION_FOLLOWING`; latency recorded per response.<br>**Result:** the cheaper model scores within 0.02 and answers in half the time. Its id goes in `config.py` <span>illustrative</span> | [evaluate-agents](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/evaluate-agents); source slides 17, 40 | tour | 1 |
| 3 | **An eval case is a recorded conversation with the expected trajectory inside it.** `EvalSet → eval_cases[] → conversation[] → invocation`: `user_content` (the prompt), `final_response` (the reference answer), `intermediate_data.tool_uses[]` (name and args, in order), `intermediate_data.tool_responses[]` (what each tool returned), plus `session_input` (app, user, initial state). Reference-based metrics read `final_response` and `tool_uses`; reference-free metrics need only the prompt and what the agent did. The file is code: it lives in the repo and changes in a reviewed diff | **yes**: eval sets are maintained artifacts; a case records a trajectory, not a string | **Actor:** Priya, writing the first case by hand.<br>**Input:** one conversation, one invocation.<br>**Mechanism:** `user_content` "refund order 6041"; `tool_uses` `[get_order{id:6041}, verify_shipment{id:6041}, issue_refund{id:6041}]`; `final_response` "Your refund of $42 has been issued."<br>**Result:** fourteen lines of JSON, and the whole schema is on the page | `eval_set.py`, `eval_case.py:36-42` (2.9.0); [adk.dev/evaluate](https://adk.dev/evaluate/) schema block | focus | 1 |
| 4 | **Test file and evalset: one schema, two discoverers.** `.test.json` is for `AgentEvaluator.evaluate`; given a directory it walks it recursively for that suffix and reads `test_config.json` beside each file. `.evalset.json` is what `adk web` and `adk eval_set` create and manage, and what `adk eval` takes by path or id. Both go through `_load_eval_set_from_file`, which parses `EvalSet` first and falls back to the legacy flat `[{query, expected_tool_use, reference}]` list with a warning. The suffix decides which tool finds the file; the legacy-format question is separate and applies to either suffix | generic (a file-discovery convention) — **say so** | **Actor:** Priya.<br>**Input:** a session saved from `adk web` as `refunds.evalset.json`.<br>**Mechanism:** pytest never sees it. She copies it unchanged to `tests/eval/refunds.test.json`; `AgentEvaluator.evaluate(agent_module, "tests/eval")` finds it.<br>**Result:** same bytes, two discoverers | `agent_evaluator.py:340-346, 394-421`, `local_eval_sets_manager.py:46` (2.9.0) | focus | 1 |
| 5 | **How a trajectory becomes a number: `tool_trajectory_avg_score`.** Per invocation, the actual list of tool calls is compared with the expected list under a `match_type`: `EXACT` (default: same calls, same order, nothing extra), `IN_ORDER` (expected calls present in order, extras allowed), `ANY_ORDER`. The invocation scores **1.0 or 0.0**; the case score is the mean over invocations. Default threshold **1.0**, so one wrong invocation fails the case. What it does not see: the metric reads only tool names and arguments. `tool_responses` are in the case, and this metric never reads them, so a correctly named call that returned an error scores 1.0. Tool results are read by `hallucinations_v1`, by the rubric-based tool-use judges, and by the service's `TOOL_USE_QUALITY` (loss patterns *Tool Failure*, *Tool Error*). Correction to the source and to the companion page: no per-step credit on 2.9.0 | **yes**: multi-dimensional correctness; the trajectory is graded separately from the answer and from tool success | **Actor:** Marco, reading a failed case.<br>**Input:** concept 1's case with two invocations: "refund 6041", then "and 6042".<br>**Mechanism:** invocation 1 matches expected: 1.0. Invocation 2 skipped `verify_shipment`: 0.0. Case score 0.5, threshold 1.0.<br>**Result:** fail. Under `IN_ORDER` with an extra `get_customer` call between steps: still 1.0, because the three expected calls appear in order <span>computed from the 2.9.0 code path</span> | `trajectory_evaluator.py:106-173`, `eval_metrics.py:193-240`, `eval_config.py:237-239`, `eval_case.py:197-222` (2.9.0); [criteria](https://adk.dev/evaluate/criteria/) | focus | 1 |
| 6 | **Grading the answer: shared words, or a judge.** `response_match_score` is ROUGE-1 F-measure between `final_response` and the reference: tokens `[a-z0-9]`, Porter-stemmed, default threshold **0.8**. It counts shared words. Any reply that reuses enough of the reference's words scores high, whatever it means; a correct reply in different words scores low. `final_response_match_v2` sends the reply and the reference to a judge model with the question "valid or invalid", samples the judge `num_samples` times, majority-votes each invocation, and reports the fraction of valid invocations. The judge is a model call with its own non-determinism; the samples exist for that reason | generic (text similarity vs. model judgment) — **say so**; the agent-shaped part is that the judge is another non-deterministic model | **Actor:** Priya, choosing an answer metric.<br>**Input:** reference "Your refund of $42 has been issued."<br>**Mechanism:** reply A, "I have issued your $42 refund": ROUGE-1 F 0.615. Reply B, "Your refund of $42 has been denied": ROUGE-1 F 0.857.<br>**Result:** under `response_match_score` at 0.8, A fails and B passes. `final_response_match_v2` marks A valid and B invalid <span>computed with rouge_score 2026-09-13</span> | `final_response_match_v1.py:68-69, 180-199`, `final_response_match_v2.py:134-136, 196-209` (2.9.0); scratchpad run | focus | 1 |
| 7 | **The thirteen ADK criteria, sorted by what each needs.** Page 1 is one table: needs a reference? needs rubrics? uses a judge? runs locally or delegates to the Agent Platform evaluation service. Five delegate: `safety_v1`, `response_evaluation_score`, `multi_turn_task_success_v1`, `multi_turn_trajectory_quality_v1`, `multi_turn_tool_use_quality_v1`; they need `GOOGLE_CLOUD_PROJECT` and a supported region. Most used: the two defaults, then `final_response_match_v2`, `hallucinations_v1`, and the three `multi_turn_*` for simulated conversations. Page 2 picks by scenario: fixed refund flow → `tool_trajectory_avg_score` with `IN_ORDER` plus `final_response_match_v2`; RAG answer → `hallucinations_v1`; customer-facing tone → `rubric_based_final_response_quality_v1` with three rubrics; open-ended multi-turn → `multi_turn_task_success_v1` under user simulation, where reference-based metrics are not supported | **yes**: multi-dimensional correctness; and the delegating five make the eval itself depend on an external service with regions and quotas | **Actor:** Marco, with a RAG agent.<br>**Input:** the agent answers "the return window is 45 days"; the retrieved policy says 30.<br>**Mechanism:** `response_match_score` needs a single reference sentence, and there is none. `hallucinations_v1` splits the reply into claims and checks each against the tool output.<br>**Result:** one of two claims unsupported: 0.5 <span>illustrative</span> | `metric_evaluator_registry.py:206-260`; `*_evaluator.py` imports of `vertex_ai_eval_facade` (2.9.0); [criteria](https://adk.dev/evaluate/criteria/) table | focus | 2 |
| 8 | **A rubric is one testable criterion. The score is the fraction that passed.** The same shape appears in three places. ADK's `rubric_based_final_response_quality_v1`, `rubric_based_tool_use_quality_v1`, and `rubric_based_multi_turn_trajectory_quality_v1`: you write the rubrics (`rubric_id`, `rubric_content.text_property`, optional `type`), in the config or on the case; the ADK judge marks each Pass/Fail per invocation and reports the fraction. The service's adaptive metrics: the rubrics are generated from the agent's own instruction and tool declarations plus the prompt, each gets Pass/Fail with a rationale, and the score is the fraction passed. Custom LLM metrics (`types.LLMMetric`): a prompt template you write, registered once. Static metrics (`SAFETY`, `HALLUCINATION`) return one 0–1 number instead | **yes**: the rubrics are derived from, or written against, the agent's configuration, which is code | **Actor:** Priya, writing rubrics for the refund agent.<br>**Input:** three rubrics: "states the refund amount", "does not promise a delivery date", "under three sentences".<br>**Mechanism:** `rubric_based_final_response_quality_v1` in `test_config.json` with those three; the judge marks each per invocation.<br>**Result:** a reply that states the amount in four sentences with no delivery promise scores 2/3 ≈ 0.67 | `eval_rubrics.py:35-65`, `rubric_based_evaluator.py:134-260` (2.9.0); [criteria](https://adk.dev/evaluate/criteria/) rubric sections; [manage-metrics](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/manage-metrics) | focus | 1 |
| 9 | **Configuring a judge, and what a run costs.** `test_config.json` (or `--config_file_path`): a bare float is a threshold; an object adds `judge_model_options` (`judge_model` default `gemini-2.5-flash`, `num_samples` default 5), `match_type` for trajectory, `rubrics[]` for the rubric criteria. `AgentEvaluator` runs every case `num_runs=2` times by default. Cost, one bullet: one case with one judge metric is 2 agent runs plus 10 judge calls, before the agent's own tool and model calls | **yes**: non-determinism is why `num_runs` and `num_samples` exist; per-call cost is why the defaults are worth knowing | **Actor:** Priya, sizing the PR set.<br>**Input:** 60 cases, `final_response_match_v2` at defaults.<br>**Mechanism:** 60 × 2 runs = 120 agent runs; 120 × 5 samples = 600 judge calls per pipeline run.<br>**Result:** `num_samples: 3` for the PR set and 5 for the nightly set: 360 judge calls on a PR <span>computed from defaults</span> | `eval_metrics.py:81-115`, `agent_evaluator.py:73`, `eval_config.py:97-135` (2.9.0); [criteria](https://adk.dev/evaluate/criteria/) config samples | focus | 1 |
| 10 | **A score is not a pass until something compares it to the threshold.** `AgentEvaluator.evaluate` is `async`; awaited inside a pytest test it raises `AssertionError` naming the metric, the threshold, and the value, and the test fails. `adk eval` prints `Tests passed / Tests failed` and stops there. That is the one fact this module needs. Module 2 places the comparison in the pipeline, covers the CLI's exit code, and sizes the sets | generic (assert vs. print) — **say so** | **Actor:** Marco, adding the first gate.<br>**Input:** concept 5's failing case.<br>**Mechanism:** under pytest: `E   AssertionError: tool_trajectory_avg_score for app Failed. Expected 1.0, but got 0.5.` Under `adk eval`: `Tests failed: 1`.<br>**Result:** exit code 1 and 0 respectively <span>reproduced on 2.9.0 in M2, page 6</span> | `agent_evaluator.py:553-575, 844-887`; `cli_tools_click.py:1290-1520` (no exit on failure) (2.9.0) | focus | 1 |
| 11 | **Two ways to grow a set: capture real conversations, or generate scenarios.** Capture: in `adk web`, chat until the agent does the right or wrong thing, save the session as a case, edit the expected trajectory and reference. Every production failure and bug report becomes a case. Generate: `adk eval_set generate_eval_cases` (also `agents-cli eval dataset synthesize`, and `client.evals.generate_conversation_scenarios`; one service call) writes `ConversationScenario` cases with a starting prompt, a plan, and a persona, and **no expected trajectory**, so they work only with reference-free metrics under user simulation, and need a project. The set is versioned with the agent; a version ships when its scores are at or above the last accepted version's on the same set. Cover ambiguous inputs, tool errors, and out-of-scope requests | **yes**: eval sets are maintained artifacts; captured cases are recorded trajectories | **Actor:** Priya, after a customer was refunded twice.<br>**Input:** "refund 6041" sent twice in one `adk web` session.<br>**Mechanism:** save the session as a case; edit the second invocation's expected `tool_uses` to `[get_order]` and its reference to say the refund already exists.<br>**Result:** the case fails until the instruction is fixed, then guards it. Separately, `generate_eval_cases` with `count: 20` and "users asking about refunds for unshipped orders" adds twenty scenario cases for the simulated-user run | `adk_web_server.py` eval-set routes (`.../eval-sets/{id}/add-session`), `cli_tools_click.py:1645-1895` (2.9.0); [user-sim](https://adk.dev/evaluate/user-sim/); companion page 34 | focus | 1 |
| 12 | **Three layers, not four systems.** ADK eval is the local harness; five of its metrics delegate to the Agent Platform evaluation service, passing the accessor the service docs name (`MultiTurnTaskSuccessV1Evaluator` sends `vertexai.types.RubricMetric.MULTI_TURN_TASK_SUCCESS`). The service is the managed scorer plus the production half ADK has no equivalent for: runs over historical traces, Online Monitors, loss clustering, a metric registry. agents-cli is a third front end over the service, with its own dataset format. The source's "model eval vs. agent eval" table is a false split; the six trajectory matches it lists are `v1beta1 evaluateInstances` inputs and are absent from the current metric set | **yes**: the model is an external dependency, and so is the scorer; v2/v3 metric specs need Gemini 3.5 Flash and are not offered in every region (`us-central1` named) | **Actor:** Marco, running `multi_turn_task_success_v1` locally.<br>**Input:** no `GOOGLE_CLOUD_PROJECT` set.<br>**Mechanism:** the facade raises "specify both project id and location". He sets project and `us-central1`; a v2 spec is unavailable there, so he pins `version="v1"`.<br>**Result:** one metric, reachable three ways: ADK config, `client.evals.evaluate`, `agents-cli eval grade` | `multi_turn_task_success_evaluator.py:60-62`, `vertex_ai_eval_facade.py:40-60` (2.9.0); `agentplatform/_genai/_evals_constant.py` (1.165.1); `aiplatform_v1beta1/types/evaluation_service.py:357-377` | focus | 1 |
| 13 | **ADK criteria and service metrics: the same names, three relationships, and the harness decides which you use.** One table, name by name. Same thing (ADK sends your invocations to the service): `multi_turn_task_success_v1` = `MULTI_TURN_TASK_SUCCESS`, `multi_turn_tool_use_quality_v1` = `MULTI_TURN_TOOL_USE_QUALITY`, `multi_turn_trajectory_quality_v1` = `MULTI_TURN_TRAJECTORY_QUALITY`, `safety_v1` = `SAFETY`, `response_evaluation_score` = `COHERENCE`. Same idea, different scorer (ADK runs its own judge locally on your model; the service runs its own with adaptive rubrics): `hallucinations_v1` / `HALLUCINATION`, `rubric_based_tool_use_quality_v1` / `TOOL_USE_QUALITY`, `final_response_match_v2` / `FINAL_RESPONSE_MATCH`. ADK-only, no model call: `tool_trajectory_avg_score`, `response_match_score`. Rule: ADK criteria wherever the harness is ADK (the dev loop, pytest, `adk eval`: a local agent module and recorded cases). Service metrics wherever the harness is the service (a deployed agent, production traces, monitors, loss clustering). Development and pre-merge use ADK; production uses the service, because ADK's harness cannot read production traffic | **yes**: the same model-as-dependency fact from concept 12, now as a decision | **Actor:** Priya, asked why the PR gate and the production dashboard report different hallucination numbers.<br>**Input:** `hallucinations_v1` in `test_config.json`; `HALLUCINATION` on the Online Monitor.<br>**Mechanism:** the first is ADK's sentence-level judge on `gemini-2.5-flash` over recorded cases; the second is the service's claim-level judge over sampled production traces.<br>**Result:** two scorers, two populations. The numbers are not comparable; each is compared with its own previous value | files cited on concepts 7 and 12; [manage-metrics](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/manage-metrics) | focus | 1 |
| 14 | **The service's seven agent metrics, and what a run returns.** Single-turn: `FINAL_RESPONSE_QUALITY`, `TOOL_USE_QUALITY` (adaptive), `HALLUCINATION`, `SAFETY` (static). Multi-turn: `MULTI_TURN_TASK_SUCCESS`, `MULTI_TURN_TOOL_USE_QUALITY`, `MULTI_TURN_TRAJECTORY_QUALITY` (adaptive). `client.evals.evaluate(dataset, metrics=[types.RubricMetric.…])` for a notebook; `create_evaluation_run` for a server-side run that writes results to a bucket and appears in the console. Each adaptive rubric comes back with its text, an importance, a verdict, and a reasoning sentence. Custom metrics register once with `create_evaluation_metric` and apply to runs and monitors alike | **yes**: multi-dimensional correctness; the rubrics are generated from the agent's configuration | **Actor:** the M3 weather agent, one production trace.<br>**Input:** "What's the weather in London?"; the agent called `get_weather(city="London")` and replied "The weather in London is currently 15°C and drizzling."<br>**Mechanism:** `create_evaluation_run` with `TOOL_USE_QUALITY(version="v1")` and `FINAL_RESPONSE_QUALITY(version="v1")`. The service generated rubrics from the agent's instruction and tool declarations: "calls `get_weather` for a current-weather question, as the developer instructions specify"; "the `city` parameter is set to London"; "the final answer is one or two sentences long".<br>**Result:** `final_response_quality_v1` 1.0 (three rubrics, three pass); `tool_use_quality_v1` 0.75 (four rubrics, three pass). 23 seconds. **Observed 2026-09-13** | `scratchpad/m4_score_run.py` output; [manage-metrics](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/manage-metrics); [view-results](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/view-results) | focus | 1 |
| 15 | **agents-cli: what it adds, and the SDK call behind each.** `eval generate` (run the agent, write traces; `--url` targets a Cloud Run, GKE, or local server) → `eval grade` (score via the service) → `eval run` (both); `eval analyze` (loss clusters), `eval compare` (diff two result files), `eval dataset synthesize` (scenario cases plus full simulated runs), `eval submit` (managed cloud run), `eval metric list`. `analyze`, `dataset`, and `submit` are marked Experimental in 1.5.0. Unique to the CLI: none of the scoring. The conveniences are `compare` (a JSON diff) and the pre-wired `tests/eval/` scaffold with a local judge. Without it: `client.evals.run_inference`, `.evaluate`, `.generate_loss_clusters`, `.generate_conversation_scenarios`, and a diff. Its dataset shape (`eval_case_id`, `prompt`, `reference.response`) is a third format, not ADK's | generic (a CLI front end) — **say so** | **Actor:** Priya, on a scaffolded project.<br>**Input:** `tests/eval/eval_config.yaml` with `custom_response_quality`, a local 1–5 judge in `response_quality.py`, a three-case dataset.<br>**Mechanism:** `agents-cli eval run` writes `results_<timestamp>.json`; `eval compare` against last week's file.<br>**Result:** `+0.07` on quality and `-1` turn. Nothing in the scaffold's pipeline calls it (M2 page 9) | `google/agents/cli/eval/cmd_*.py`, `scaffold/agents/adk/tests/eval/*` (1.5.0); M2 page 8 | focus | 1 |
| 16 | **Where evaluation runs across the agent's life.** Three moments, same metrics, different data source. While developing: `adk web`, a handful of cases, you read the scores. Before merge: pytest over the PR set, the threshold is the gate; Module 2 places it. In production: the service scores real traffic, on demand over history and continuously on a schedule. Behavior changes with frozen code when the model behind an alias changes or the input mix shifts, so production scoring is not optional | generic lifecycle table — **say so**; agent-shaped in the last row: the model is an external dependency that changes under you | **Actor:** Marco.<br>**Input:** the agent passed its PR gate on 2026-08-30; nothing merged since.<br>**Mechanism:** on 2026-09-10 the production monitor's task success falls from 0.91 to 0.78; the model alias had moved to a new version.<br>**Result:** the eval set never ran, because nothing merged. Only the production run saw it <span>illustrative</span> | [agent-evaluation](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/agent-evaluation) workflow table; [evaluate-agents](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/evaluate-agents) three-type table | focus | 1 |
| 17 | **How a production trace becomes something the service can grade, per runtime.** What must exist: an `invoke_agent` span with `gen_ai.agent.name`, `gen_ai.agent.description`, `gen_ai.conversation.id`, and the `gen_ai.client.inference.operation.details` event carrying input and output messages, system instruction, and tool definitions. The ADK OTel exporter writes those when `OTEL_SEMCONV_STABILITY_OPT_IN=gen_ai_latest_experimental` and `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=EVENT_ONLY` are set (Module 3 pages 18–21). Agent Runtime: set when Cloud Trace is enabled; the console's Evaluation and Online monitors pages read it directly, and the documented prerequisite is "a working Agent Runtime deployed with Cloud Trace enabled". Cloud Run and GKE: you set the two variables and export to Cloud Trace yourself; the console pages do not list your agent (the Cloud Run Agent Platform page names only Agent Identity and Agent Registry, both Preview; nothing for GKE). What does work from any runtime: `client.evals.import_evaluation_set(cloud_trace_source={project_id, trace_ids | session_ids})`, which takes no agent resource. Also runtime-independent: the exporter's GCS upload (`OTEL_INSTRUMENTATION_GENAI_COMPLETION_HOOK=upload`, `…UPLOAD_BASE_PATH=gs://…`) read back with `EvaluationDataset.load_from_observability_eval_cases`; and `agents-cli eval generate --url` for fresh traffic, not history. Those traces carry every user message | **yes**: trajectory-level observability carries user content; the model is an external dependency and so is the scorer | **Actor:** the M3 weather agent, run locally with `adk web --otel_to_cloud` (resource `generic_task`, job `adk-metrics`), not on Agent Runtime.<br>**Input:** trace `002b92f8…` from 2026-09-13: the three span attributes present; log events in the older per-message form (`gen_ai.user.message`, `gen_ai.choice`), not `inference.operation.details`.<br>**Mechanism:** `import_evaluation_set(cloud_trace_source={"project_id": "jwd-gcp-demos", "trace_ids": [...]})` in `us-central1`.<br>**Result:** `importedItemCount: 1` in 3 seconds. The item held the agent's instruction and tool declarations, the prompt, the `get_weather` call and its response, and the final text; the session's four invocations landed under one turn with the prompt once. **Observed 2026-09-13.** Whether the console would list this agent: no; it has no reasoning engine id | `scratchpad/m4_trace_import.py` output; [evaluate-offline](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/evaluate-offline) prerequisites and telemetry sections; [Cloud Run Agent Platform features](https://docs.cloud.google.com/run/docs/ai/agent-platform-features); `agentplatform/_genai/evals.py:3945-3996`, `types/common.py:6871-6884, 4172-4225` (1.165.1); M3 pages 18–21 | focus | 1 |
| 18 | **Production, on demand: run the offline evaluation.** Console (Agent Runtime): Evaluation page → New evaluation → Traces or Sessions → filter by version or time → a bucket path, entered once → Evaluate agent. The four default metrics are added; each failing rubric links to its trace. SDK (any runtime): import the traces into an evaluation set (concept 17), then `create_evaluation_run(dataset=EvaluationRunDataSource(evaluation_set=…), metrics=[…], dest="gs://…")`; results are summary statistics per metric on the run and a per-item JSON in the bucket. A trace is one execution path; a session is the whole conversation | **yes**: the same user-content fact as 17, now at the point where a team chooses which two weeks to score | **Actor:** Marco, after the drop in concept 16.<br>**Input:** "Last 2 weeks", version 7, about 400 traces.<br>**Mechanism:** the four default metrics run; results land in the bucket and the Evaluations tab.<br>**Result:** `HALLUCINATION` unchanged; `TOOL_USE_QUALITY` fell. He opens the failing rubrics' traces <span>illustrative counts; the mechanism is the one observed on concept 14</span> | [evaluate-offline](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/evaluate-offline); `agentplatform/_genai/evals.py:3539-3600` (1.165.1); `scratchpad/m4_score_run.py` output | focus | 1 |
| 19 | **Production, continuously: an Online Monitor.** An `OnlineEvaluator` resource runs a loop, typically every 10 minutes: query (sample a percentage of live traces from Cloud Trace and Cloud Logging, capped by max samples per run, optionally filtered by duration or token usage) → evaluate (the configured metrics, via the service) → report (results to Cloud Logging; numeric scores to Cloud Monitoring). Agent Runtime only: the monitor is created against an "Agent engine" from a dropdown. Security disclosure from the docs: the monitor runs as the project service agent, so anyone who can create one can attach it to any agent in the project; restrict creation | **yes**: non-determinism plus the model as an external dependency mean frozen code does not mean frozen behavior; the sampled traces carry user content | **Actor:** Priya, setting up the refund agent's monitor.<br>**Input:** 10% sampling, 50 max per run, `MULTI_TURN_TASK_SUCCESS` and `SAFETY`.<br>**Mechanism:** six runs an hour.<br>**Result:** up to 300 scored conversations an hour, each visible in Logging under `labels.reasoning_engine_id` <span>illustrative sizing</span> | [evaluate-online](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/evaluate-online) | focus | 1 |
| 20 | **Alerting on quality is an ordinary Cloud Monitoring policy.** The monitor exports `aiplatform.googleapis.com/online_evaluator/scores` with label `evaluation_metric_name`; a policy with `COMPARISON_LT`, a threshold, a `duration`, and `ALIGN_MEAN` over 60 s notifies a channel. Offline runs export nothing to Monitoring, so alerts require a monitor. Same mechanism as Module 3's outcome-counter alert, on a score instead of a count | generic (Cloud Monitoring) — **say so** | **Actor:** Marco, on call.<br>**Input:** the docs' `policy.yaml`: `task_success` below 0.8 for 1800 s → a notification channel.<br>**Mechanism:** on 2026-09-10 the 30-minute mean reads 0.78.<br>**Result:** the incident opens with a link to the sampled traces | [quality-alerts](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/quality-alerts) `policy.yaml`; M3 page 28 | focus | 1 |
| 21 | **Reacting: cluster the failures, turn them into cases, compare versions.** Loss analysis classifies each failed rubric into a fixed taxonomy (13 patterns for task success, for example *Omission of Required Tool Call* and *Over-Punting*; 13 for tool use) and groups them. Triage: summary → per-case → clusters → traces → root cause (prompt, tool definition, or data) → fix → re-run and compare. The failing traces become eval cases (concept 11); the comparison is `eval compare` or the dashboard's Performance Trends across versions | **yes**: eval sets are maintained artifacts, and production is where the new cases come from | **Actor:** Marco.<br>**Input:** 40 failures from the offline run.<br>**Mechanism:** `generate_loss_clusters`: *Omission of Required Tool Call*, 31 of 40, all missing `verify_shipment`. He adds three of those conversations to the set and restores the instruction clause.<br>**Result:** the PR set fails, then passes; the next monitor window reads 0.90 <span>illustrative</span> | [view-results](https://docs.cloud.google.com/gemini-enterprise-agent-platform/optimize/evaluation/view-results) loss taxonomy and triage list; `cmd_analyze.py`, `cmd_compare.py` (1.5.0) | focus | 1 |
| 22 | **Optional: conformance testing is replay, not scoring.** `adk conformance record` runs the agent with a recordings plugin and writes `generated-recordings.yaml` (every LLM request and response, every tool call and result) and `generated-session.yaml`. `adk conformance test` (replay, the default) reruns the agent against the recording and fails on any difference: event count, request, response, tool call. No model call, no score, no threshold: a deterministic regression test whose fixture is the model's recorded behavior. `record` and `--mode live` are both marked work in progress in 2.9.0 | generic (record/replay) — **say so**; the agent-shaped fact is that the thing being mocked is the model | **Actor:** Priya, then Marco.<br>**Input:** the refund case, recorded once.<br>**Mechanism:** Marco renames `verify_shipment` to `check_shipment`; replay reruns the case.<br>**Result:** failure at the first tool call, with a unified diff of the recorded vs. actual function call. Nothing was graded; something changed | `cli/conformance/cli_test.py`, `_replay_validators.py:124-141`, `cli_tools_click.py:568-700` (2.9.0); [adk.dev/evaluate](https://adk.dev/evaluate/) conformance section | optional | 1 |
| 23 | **Optional: related topics, one page.** User simulation (a `ConversationScenario` with `starting_prompt`, `conversation_plan`, `user_persona` from `EXPERT` / `NOVICE` / `EVALUATOR`; reference-based metrics unsupported); environment simulation (mock or inject tool failures; experimental); `adk optimize` (GEPA rewrites the root instruction against an eval set; experimental); custom metrics (a Python function with the `EvalMetric, actual, expected, scenario → EvaluationResult` signature, registered under `custom_metrics`) | mixed; each labeled on the face | Four rows, one per topic:<br>**Persona:** `NOVICE` cannot correct the agent, so a wrong refund amount goes unchallenged.<br>**Environment:** a 503 injected on `verify_shipment`; the rubric expects the agent to say so.<br>**Optimize:** the instruction rewritten after 100 metric calls; validation score reported.<br>**Custom metric:** the refund amount in the reply equals the tool's return | [user-sim](https://adk.dev/evaluate/user-sim/), [environment_simulation](https://adk.dev/evaluate/environment_simulation/), [optimize](https://adk.dev/optimize/), [custom_metrics](https://adk.dev/evaluate/custom_metrics/); `simulation/pre_built_personas.py:460-510` (2.9.0) | optional | 1 |

**Total content pages: 24** = 22 required + 2 optional (+ opener, objectives, 4 dividers, 3 recaps, consequences, references = **11 structural**) → **35 pages**. Required content at 2.5 min/page ≈ 55 min against the 50-min budget; the optional section adds ≈ 5 min. Jeff, 2026-09-13: no folds; the overage stands.

## Order and why

The spine is: **a decision, recorded → graded → made a gate → graded again in production → fed back into the set.**

Section 1 (concepts 1–11, "What gets graded"). The trajectory regression (1) opens because every later mechanism exists to catch it. Model selection (2) follows so that "model eval" is settled before the agent pages. The case format (3) precedes any metric because every metric reads its fields; test-file vs. evalset (4) follows while the schema is on screen. Trajectory scoring (5) before answer scoring (6): it is the page that makes "grading a decision" concrete, and it reuses the case from 1. With two metrics understood, the full table (7) is readable, and its second page picks by scenario. Rubrics (8) come next because the table's "needs rubrics" and "uses a judge" columns need a definition, and because the same shape returns in section 2. Judge configuration and cost (9) follows rubrics. Threshold-to-pass (10) closes the mechanics and hands pipeline placement to Module 2. Building the set (11) closes the section because it needs the case format, the metrics, and the failure mode to explain what "edit the expected trajectory" means, and its generated-scenario half needs the reference-free distinction from 7.

Section 2 (concepts 12–15, "The managed layer"). The three-layers diagram (12) needs section 1's "five delegate" fact. The name-by-name mapping and the harness rule (13) need 12. The service's seven metrics and what a run returns (14) need 8 and 13. agents-cli (15) is a front end over what was just shown.

Section 3 (concepts 16–21, "In the lifecycle"). The three-moments table (16) frames the section. Then production in the order a team meets it: how a trace becomes gradable, per runtime (17); the offline run (18); the monitor (19); the alert (20); the reaction (21), which closes the loop back to 11. Section 3 restates the two OTel variables on the face and refers to Module 3 for how they were set; it refers to Module 2 for the gate and does not re-teach it.

Section 4 (22–23) is optional and follows the section-3 recap, so it can be dropped without touching the spine.

## Cut from the source, and why

- **Slides 2–3, 12, 41–44** (objectives, topics ×3, lab, Q&A, thank you): navigation.
- **Slides 4–11** (testing an LLM function, LLM-as-judge on a marketing post): generic LLM-application testing; one sentence on concept 2. Their code no longer imports (`vertexai.generative_models` removed 2026-06-24).
- **Slides 13–16** (three-word definition, "why critical", maturity model, business challenges): replaced by concept 1's worked regression and concept 16's lifecycle table.
- **Slide 18** (Exact Match / ROUGE / BLEU table): `response_match_score` gets its mechanism on concept 6; the rest is notes.
- **Slides 19–21** (agent metrics, model-based metrics): rebuilt as concepts 7 and 14 from the current metric sets; the source's list is the retired one (`Fulfillment` gone).
- **Slide 23** (Evaluation API four steps): folded into concept 14's code.
- **Slide 25** (`determine_intent`, `use_tool`, `review_results`, `report_generation`): invented field names; concept 5 replaces it.
- **Slide 26** (six trajectory matches): `v1beta1` inputs not in the current metric set; noted on concept 12, replaced by the three ADK match types on concept 5.
- **Slides 27–31** (test file vs. evalset ×4, "creating evalsets is complex"): concept 4, one page.
- **Slides 35–38** (three ways to run, CLI screenshot, pytest, CI pipeline): Module 2 owns them; concept 10 keeps the one fact and the corrected `await`.
- **Slide 40** (model eval vs. agent eval table): a false split; concepts 12–13 replace it.

## Open questions for Jeff

None outstanding (2026-09-13: no folds, overage accepted; console scope for Cloud Run/GKE stated as documented and as measured).
